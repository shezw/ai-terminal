## 场景

model config 交互式配置

### 开发目标

#### 1. 开发内容

- 为 `ai-terminal model` 增加 `config` 子命令与交互式输入流程
- 将输入内容写回现有 `config.yaml`，并补充必要测试与帮助文档

#### 2. 预期结果

- 执行 `ai-terminal model config` 时，用户可按提示配置 API 或本地模型相关字段并成功保存

#### 3. 验证方式

- `go test ./...`
- `go run ./cmd/ai-terminal model config` 交互式冒烟验证
